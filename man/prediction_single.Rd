% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/main.R
\name{prediction_single}
\alias{prediction_single}
\title{Predict m6A modification for a single sequence}
\usage{
prediction_single(
  ml_fit,
  gc_content,
  RNA_type,
  RNA_region,
  exon_length,
  distance_to_junction,
  evolutionary_conservation,
  DNA_5mer,
  positive_threshold = 0.5
)
}
\arguments{
\item{ml_fit}{Fitted \code{randomForest} model (same as in
\code{\link[=prediction_multiple]{prediction_multiple()}}).}

\item{gc_content}{Numeric. GC content of the region.}

\item{RNA_type}{Character (will be converted to factor). One of
\code{"mRNA"}, \code{"lincRNA"}, \code{"lncRNA"}, \code{"pseudogene"}.}

\item{RNA_region}{Character (will be converted to factor). One of
\code{"CDS"}, \code{"intron"}, \code{"3'UTR"}, \code{"5'UTR"}.}

\item{exon_length}{Numeric. Length of the exon.}

\item{distance_to_junction}{Numeric. Distance to nearest splice junction.}

\item{evolutionary_conservation}{Numeric. Conservation score.}

\item{DNA_5mer}{Character. 5-mer DNA sequence centered on the site.}

\item{positive_threshold}{Numeric (default \code{0.5}). Classification threshold.}
}
\value{
A \strong{named vector} with two elements:
\item{predicted_m6A_prob}{Rounded probability of \code{"Positive"} (4 decimals)}
\item{predicted_m6A_status}{Character \code{"Positive"} or \code{"Negative"}}
}
\description{
Convenience wrapper around \code{\link[=prediction_multiple]{prediction_multiple()}} for a single set
of feature values supplied as separate arguments.
}
\examples{
# 加载模型
rf_model <- readRDS(system.file("extdata", "rf_fit.rds", package = "m6APrediction"))

# 单个位点预测
prediction_single(
  ml_fit = rf_model,
  gc_content = 0.48,
  RNA_type = "mRNA",
  RNA_region = "CDS",
  exon_length = 120,
  distance_to_junction = 30,
  evolutionary_conservation = 0.92,
  DNA_5mer = "GACTA"
)

}
