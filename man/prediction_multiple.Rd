% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/main.R
\name{prediction_multiple}
\alias{prediction_multiple}
\title{Predict m6A modification for multiple sequences}
\usage{
prediction_multiple(ml_fit, feature_df, positive_threshold = 0.5)
}
\arguments{
\item{ml_fit}{A fitted \code{randomForest} model that was trained with a
factor response having levels \code{"Negative"} and \code{"Positive"}.}

\item{feature_df}{Data frame containing the required feature columns
(see \strong{Details}).  The column \code{DNA_5mer} must hold character strings
of length 5.}

\item{positive_threshold}{Numeric scalar (default \code{0.5}).  Probability
above this threshold is classified as \code{"Positive"}.}
}
\value{
A data frame with the original columns (except \code{DNA_5mer},
which is replaced by one-hot columns \code{nt_pos1} … \code{nt_pos5}) plus
two new columns:
\describe{
\item{predicted_m6A_prob}{Rounded probability (4 decimals) of class
\code{"Positive"}}
\item{predicted_m6A_status}{Factor with levels \code{"Negative"},
\code{"Positive"}}
}
The column order is fixed as shown in the source code.
#'
}
\description{
Given a fitted randomForest model and a data frame of features,
this function returns the original features together with two new columns:
\code{predicted_m6A_prob} (probability of being Positive) and
\code{predicted_m6A_status} (\code{"Positive"} or \code{"Negative"}).
}
\details{
The input \code{feature_df} \strong{must} contain the following columns
(exact names):
\itemize{
\item \code{gc_content}
\item \code{RNA_type}      (factor with levels \code{mRNA}, \code{lincRNA},
\code{lncRNA}, \code{pseudogene})
\item \code{RNA_region}    (factor with levels \code{CDS}, \code{intron},
\verb{3'UTR}, \verb{5'UTR})
\item \code{exon_length}
\item \code{distance_to_junction}
\item \code{evolutionary_conservation}
\item \code{DNA_5mer}      (character, length 5)
}
}
\examples{
# 加载包内示例数据
rf_model <- readRDS(system.file("extdata", "rf_fit.rds", package = "m6APrediction"))
input_df <- read.csv(system.file("extdata", "m6A_input_example.csv", package = "m6APrediction"))

# 批量预测
predictions <- prediction_multiple(rf_model, input_df)
head(predictions)

}
